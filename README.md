# Mahmoud Okal Portfolio

Upload `index.html`, `style.css`, and `script.js` to the root of your GitHub repository.

All portfolio images are embedded inside `index.html`, so no separate assets upload is required and image paths cannot break on GitHub Pages.
